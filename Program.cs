using System;
using System.IO;

namespace Tag
{
    class Program
    {
        static void Main(string[] args)
        {
            var directory = Directory.GetCurrentDirectory();
            Console.WriteLine(directory);
        }
    }
}
